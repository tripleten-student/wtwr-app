/**
 * The **config** module contains the variables for configurations
 *
 * @author [Devin Jaggernauth](https://github.com/mentalcaries)
 */

const localdb = 'mongodb://localhost:27017/wtwr';
const devkey = 'dev-secret key';

module.exports = { localdb, devkey };
